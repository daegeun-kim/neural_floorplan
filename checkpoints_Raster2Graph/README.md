# Raster-to-Graph Checkpoint

Place the pretrained Raster-to-Graph checkpoint here for local Phase 4 inference:

```txt
checkpoints_Raster2Graph/checkpoint0299.pth
```

The checkpoint file is intentionally not committed because it is large. Keep it local or manage it with Git LFS if the project later needs versioned model weights.
