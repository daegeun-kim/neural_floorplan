# Spec v008: Mask-to-Vector Reconstruction for Neural Floorplan

## 0. Purpose

This document defines the first mask-to-vector reconstruction stage for Neural Floorplan.

The goal is to convert a noisy 5-class CNN prediction into a clean SVG preview using the component primitives defined in spec v007.

Pipeline position:

```text
CNN predicted segmentation
→ class masks
→ mask cleanup
→ primitive candidate extraction
→ primitive fitting
→ SVG preview output
```

## 1. Scope

This spec covers:

```text
- reading CNN prediction images
- decoding 5-class predicted masks
- ignoring icon class
- extracting wall, opening, and room evidence
- fitting clean primitives
- separating door/window candidates heuristically
- generating SVG output
```

This spec does not cover:

```text
- CNN training
- CNN evaluation
- creating new CNN classes
- retraining the model
- room adjacency graph construction
- door topology validation
- final DXF export
- Grasshopper integration
- JSON export
```

## 2. Input Data

Initial input source:

```text
C:\Users\kdgki\Desktop\MSCDP\Projects\neural_floorplan\runs\segformer_b0_v005\previews
```

Use files whose names include:

```text
prediction
```

Example:

```text
sample_000_prediction.png
```

The first implementation should support prediction preview images generated by the CNN evaluation pipeline.

Important: if preview images are color-coded RGB visualizations rather than class-ID masks, the vectorizer must decode the color palette into class IDs before processing. If the palette is unavailable or inconsistent, the script should raise a clear error rather than silently producing wrong geometry.

## 3. Active Classes

Input classes:

| ID | Class | v008 action |
|---:|---|---|
| 0 | background | ignore |
| 1 | wall | vectorize |
| 2 | opening | vectorize and classify heuristically |
| 3 | room | vectorize for SVG preview |
| 4 | icon | ignore |

Icon pixels should be ignored in v008.

Do not remove the icon class from the CNN inside this stage. Removing it would require a separate CNN retraining decision.

## 4. Door vs Window Strategy

The CNN currently predicts one `opening` class. It does not directly separate doors and windows.

v008 should not retrain the CNN.

Instead, v008 should attempt heuristic opening separation after extracting opening components.

Recommended initial heuristic:

```text
window_candidate:
    long and narrow component
    high aspect ratio
    aligned with host wall
    often located on exterior wall

door_candidate:
    more compact component
    aspect ratio closer to 1
    larger blob/chunk near wall break
    may have swing-like evidence if visible in original/predicted image

generic_opening:
    uncertain classification
```

If classification confidence is low, output a generic opening instead of forcing a door/window decision.

## 5. Scale and Unit Handling

The preferred output is real-world units.

However, a raster prediction alone does not reliably define real-world scale.

v008 should include a scale resolver with this priority:

```text
1. explicit scale metadata if available
2. original SVG/dataset metadata if available
3. dimension text parsed elsewhere if available
4. standard architectural assumptions, such as door width
5. fallback to pixel units
```

For the first implementation, the system may export SVG in pixel coordinates if scale is unknown.

The SVG output should record the unit status:

```text
unit="px"
scale_status="unknown"
```

or:

```text
unit="mm"
scale_status="resolved"
```

Do not silently pretend pixel units are real-world dimensions.

## 6. Output Format

The first milestone exports SVG only.

No JSON output is required in v008.

Output folder:

```text
outputs/vectorization/v008/
```

Recommended output files:

```text
sample_000_vector.svg
sample_000_debug.svg
```

The SVG should use separate layers/groups:

```text
<g id="rooms">
<g id="walls">
<g id="openings">
<g id="doors">
<g id="windows">
<g id="debug">
```

## 7. Main Pipeline

### Step 1 — Load prediction image

Input:

```text
sample_000_prediction.png
```

Output:

```text
semantic class map
```

If the image is already class-ID encoded, read class IDs directly.

If the image is color-coded, decode colors into class IDs.

### Step 2 — Split class masks

Create binary masks:

```text
wall_mask
opening_mask
room_mask
```

Ignore:

```text
background_mask
icon_mask
```

### Step 3 — Clean masks

Apply lightweight cleanup:

```text
remove tiny components
fill small holes
close small gaps
smooth jagged noise
```

Class-specific intent:

```text
wall_mask:
    preserve global wall structure
    close small gaps
    remove isolated blobs

opening_mask:
    preserve opening locations
    remove tiny fragments
    avoid over-smoothing

room_mask:
    preserve broad room regions
    simplify noisy boundaries
```

### Step 4 — Extract wall candidates

Recommended first approach:

```text
wall mask
→ skeletonization or medial axis
→ line detection
→ merge collinear segments
→ snap near-horizontal and near-vertical lines
→ WallPrimitive objects
```

Internal wall representation should use:

```text
centerline + thickness
```

Reason:

```text
- easier to host openings
- easier to snap and merge
- less sensitive to noisy wall boundaries
- more CAD-like than raw contours
```

### Step 5 — Extract opening candidates

Use connected components from `opening_mask`.

For each component, compute:

```text
bbox
area
aspect_ratio
major_axis
center_point
nearest wall candidates
alignment with wall
```

Then attach to nearest suitable wall.

An opening without a host wall should be marked unresolved in the debug SVG.

### Step 6 — Classify opening candidates

After host-wall assignment, classify each opening as:

```text
door_candidate
window_candidate
generic_opening
```

Use geometry-based heuristics only.

Do not use a new neural network in v008.

Suggested heuristic inputs:

```text
component aspect ratio
component area
component width along wall
component thickness across wall
distance to exterior boundary
host wall type if known
shape compactness
```

The first implementation should keep thresholds configurable.

Example config:

```yaml
opening_classification:
  window_min_aspect_ratio: 2.5
  door_max_aspect_ratio: 2.0
  min_confidence_for_type: 0.65
```

### Step 7 — Generate primitives

Convert candidates into v007 primitives:

```text
wall candidates       → WallPrimitive
opening candidates    → OpeningPrimitive
window candidates     → WindowPrimitive
confident door candidates → DoorPrimitive
room components       → RoomPrimitive
```

If door classification is uncertain, generate `OpeningPrimitive` only.

### Step 8 — Generate room polygons

Use room mask components to create simplified room polygons.

Room processing should remain simple in v008:

```text
room mask
→ connected components
→ contours
→ polygon simplification
→ SVG room polygons
```

Do not enforce room adjacency graph or room-to-door topology in v008.

### Step 9 — Apply basic geometric rules

v008 should include only basic geometric rules:

```text
straighten wall lines
snap near-horizontal and near-vertical walls
merge nearby collinear wall segments
remove tiny wall fragments
attach openings to nearest wall
project openings onto host wall
```

Exclude full topology validation:

```text
room adjacency graph
circulation correctness
door connects two rooms
complete room closure solving
```

These are later stages.

### Step 10 — Export SVG

Generate a clean SVG preview.

SVG should show:

```text
rooms as light polygons
walls as clean strokes or wall polygons
openings as hosted gaps/segments
doors as clean primitive symbols when confidently classified
windows as clean hosted linear symbols when confidently classified
unresolved objects in debug layer
```

## 8. Module Structure

All code should stay under `src/`.

Recommended structure:

```text
src/vectorization/
  __init__.py
  run_mask_to_vector.py
  load_prediction.py
  decode_prediction.py
  masks.py
  cleanup.py
  wall_extraction.py
  opening_extraction.py
  opening_classification.py
  room_extraction.py
  geometry_rules.py
  export_svg.py
  primitives/
    __init__.py
    base.py
    wall.py
    opening.py
    door.py
    window.py
    room.py
```

## 9. Config

Create:

```text
configs/vectorization_v008.yaml
```

Example fields:

```yaml
input:
  preview_dir: "runs/segformer_b0_v005/previews"
  filename_contains: "prediction"

output:
  output_dir: "outputs/vectorization/v008"
  export_svg: true
  export_json: false

classes:
  background: 0
  wall: 1
  opening: 2
  room: 3
  icon: 4

scale:
  mode: "auto"
  fallback_unit: "px"
  fallback_scale: 1.0

cleanup:
  min_wall_component_area: 20
  min_opening_component_area: 8
  min_room_component_area: 100
  close_wall_gap_px: 3

wall_extraction:
  snap_angle_degrees: 8
  merge_distance_px: 6
  min_wall_length_px: 10

opening_classification:
  enabled: true
  window_min_aspect_ratio: 2.5
  door_max_aspect_ratio: 2.0
  min_confidence_for_type: 0.65

svg:
  include_debug_layer: true
  draw_rooms: true
  draw_walls: true
  draw_openings: true
  draw_doors: true
  draw_windows: true
```

## 10. Door Symbol Rule

v008 may generate a simple hinged door symbol only when an opening is confidently classified as a door.

Door typology variants are excluded.

Allowed:

```text
single hinged door primitive
```

Excluded:

```text
sliding door
double door
folding door
custom door types
```

If swing direction is unknown, choose a default direction and mark the door as low confidence in the debug layer.

## 11. Window Symbol Rule

v008 may generate a simple linear window symbol when an opening is confidently classified as a window.

Window variants are excluded.

Allowed:

```text
generic wall-hosted window line/block
```

Excluded:

```text
bay window
curtain wall
sliding window variants
window family classification
```

## 12. Icon Rule

Ignore icon pixels in v008.

The icon class should not block wall, opening, or room vectorization.

If icon noise interferes with masks, it may be removed during cleanup.

## 13. Evaluation for v008

Since v008 is an early vectorization stage, evaluation should be visual and geometry-based.

Minimum visual checks:

```text
walls follow the global floorplan shape
openings are located on walls
door/window candidates are visually reasonable
room polygons broadly align with spaces
output SVG is clean enough for inspection
```

Optional quantitative checks:

```text
number of unresolved openings
percentage of openings attached to walls
average wall segment length
number of tiny fragments removed
percentage of snapped wall segments
```

## 14. First Milestone

The first working version should process 3–5 prediction preview images.

Input:

```text
runs/segformer_b0_v005/previews/*prediction*.png
```

Output:

```text
outputs/vectorization/v008/*.svg
```

Expected result:

```text
- clean wall line primitives
- simple room polygons
- hosted opening primitives
- some heuristic door/window separation
- SVG visual output
```

## 15. Acceptance Criteria

v008 is complete when:

```text
1. Prediction images can be loaded from the preview folder.
2. Class masks can be decoded reliably.
3. Wall, opening, and room masks are extracted.
4. Icon class is ignored.
5. Wall primitives are generated from noisy wall masks.
6. Opening primitives are attached to host walls when possible.
7. Door/window candidates are separated heuristically where confidence allows.
8. Room polygons are generated for SVG preview.
9. SVG output is generated for at least 3 sample predictions.
10. Unresolved/low-confidence geometry is shown in a debug layer.
```

## 16. Non-Goals

v008 does not:

```text
- retrain the CNN into 6 classes
- remove the icon class from CNN training
- guarantee perfect door/window classification
- solve full room topology
- guarantee all rooms are closed
- export JSON
- export DXF
- perform Grasshopper analysis
```

## 17. Future Extensions

Possible later specs:

```text
v009_room_topology.md
v010_door_window_refinement.md
v011_dxf_export.md
v012_grasshopper_handoff.md
```

Possible future improvements:

```text
- use original raster together with CNN prediction
- use CNN probability maps if available
- add room adjacency graph
- improve scale inference
- support multiple door/window primitive variants
- optionally retrain CNN with separate door/window classes if heuristics fail
```
